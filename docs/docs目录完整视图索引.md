## 完整核心视图清单（28项 · 带位置与生成方式）

---

### 视图01-部署架构拓扑图
  覆盖范围：整个项目（桌面端 + 手机端Web + 服务端）
  怎么生成：分析项目所有代码库的 package.json、Dockerfile、docker-compose.yml、CI 配置文件，提取出每端的运行环境、组件版本、网络依赖、部署方式，汇总成拓扑图
  作用：物理层"家底"——有几台服务器、网关在哪、数据库主从部署、服务之间内网还是公网调用、组件版本号、连接方式、数据分布策略。换工具/迁移时判断"新工具能不能塞进去"
  位置：docs/01-部署架构拓扑图.md


### 视图02-系统边界上下文图
  覆盖范围：整个项目（含所有外部依赖方）
  怎么生成：梳理项目代码中所有外部API调用（第三方支付、短信平台、外部消息中间件、统一认证中心），从配置文件（application.yml、config.json）中提取外部服务地址、协议类型、鉴权方式，汇总成边界交互图
  作用：项目"封面图"——定义系统范围，标明哪些是自建、哪些是采购、哪些是外部依赖。新接外部系统时查这张图判断对接方式和鉴权手段
  位置：docs/02-系统边界上下文图.md


### 视图03-全端技术栈依赖热点图
  覆盖范围：所有端（服务端 + 桌面端 + 手机端Web）
  怎么生成：分析各端项目的依赖清单（服务端pom.xml/gradle/build.gradle、前端package.json、客户端podspec），统计每个三方库的调用频次、依赖深度、是否有CVE漏洞，标记出不可替换的核心库
  作用：技术选型的"体检报告"——知道哪些依赖是"地基"不能动，哪些可以升级，哪些有安全隐患要替换。决定升级某个库时先查这张图看影响面
  位置：docs/03-全端技术栈依赖热点图.md


### 视图04-代码组织与开发规范视图
  覆盖范围：所有端代码仓库
  怎么生成：分析项目代码结构（src/main/java目录、各module包名）、.editorconfig、checkstyle/sonar配置文件、Git分支管理策略，提取模块划分、命名规则、包依赖方向、静态检查规则集合
  作用：代码层的"交通规则"——防止模块乱依赖、命名随心所欲、代码风格不统一。代码审查时对照这张图判断是否违规
  位置：docs/04-代码组织与开发规范视图.md


### 视图05-全端通信主链路时序图
  覆盖范围：所有端（用户操作从发起到落库的完整链路）
  怎么生成：从项目代码中追踪一个完整业务请求（如"用户下单"），从Controller→Service→DAO→Cache→MQ→Response逐层梳理，记录每层的入参出参、抛出的异常、超时配置，画出跨端调用时序
  作用：分布式系统的"电话本"——系统间怎么调、超时多少、失败了怎么办。接口联调、排查超时问题时首选这张图
  位置：docs/05-全端通信主链路时序图.md


### 视图06-并发与进程状态视图
  覆盖范围：服务端 + 客户端（进程内并发模型）
  怎么生成：分析项目代码中的线程池配置类、@Async注解使用处、CompletableFuture调用链、分布式锁（Redisson/Zookeeper）加解锁代码、订单/任务状态机枚举与流转方法，汇总成并发模型与状态迁移图
  作用：并发问题的"解谜地图"——哪个线程池满了、哪里死锁了、订单为什么卡在某个状态不动了。排查高并发下的诡异问题时掏出这张图
  位置：docs/06-并发与进程状态视图.md


### 视图07-系统静态结构与组件图
  覆盖范围：服务端（含所有业务模块与核心类）
  怎么生成：分析项目代码的包结构、顶层类继承关系、核心接口与实现类、关键设计模式（工厂/策略/模板/观察者），用类图/组件图绘制出系统的静态骨架
  作用：代码的"骨架说明书"——新功能该往哪个模块加、改某个功能会影响谁、整个项目有多少个核心组件。新入职开发第一个月反复查阅的图
  位置：docs/07-系统静态结构与组件图.md


### 视图08-服务端数据存储架构图
  覆盖范围：服务端（MySQL/Redis/ES/OSS全链路存储）
  怎么生成：分析项目代码中的数据库连接配置（master/slave地址）、MyBatis/JPA的@Table映射实体、Redis key命名规范与过期策略、Elasticsearch索引mapping与分片配置、OSS bucket划分规则，汇总成存储拓扑图
  作用：服务端数据的"藏宝图"——数据在哪个库、缓存怎么配、冷数据去哪了。加新字段/新表时查这张图决定放哪
  位置：docs/08-服务端数据存储架构图.md


### 视图09-客户端数据存储与配置图
  覆盖范围：桌面端 + 手机端Web（本地存储）
  怎么生成：分析项目代码中的SQLite建表语句（或Room Entity）、SharedPreferences/UserDefaults读写代码、本地缓存目录结构与清理策略，汇总成客户端存储全景图
  作用：客户端数据的"抽屉图"——登录信息存哪了、离线消息在哪个表、缓存文件占了多少空间。用户反馈"清理缓存后xxx不见了"时查这张图
  位置：docs/09-客户端数据存储与配置图.md


### 视图10-数据模型与生命周期治理图
  覆盖范围：所有端（全局数据模型ER + 生命周期规则）
  怎么生成：梳理所有核心业务实体（用户/订单/商品/消息/AI会话）的字段定义、关联关系、索引策略、状态流转（创建→使用→归档→销毁），形成全局ER图与数据字典
  作用：数据的"族谱"——订单表的order_status跟支付表的pay_status怎么对应、user_id在哪些表里出现、数据存多久删。跨模块联调时查这张图对齐字段含义
  位置：docs/10-数据模型与生命周期治理图.md


### 视图11-非功能需求保障视图
  覆盖范围：所有端（性能/SLA/可用性/可扩展性的技术支撑）
  怎么生成：分析项目代码中的限流配置（Sentinel/Guava RateLimiter）、熔断降级配置（Hystrix/Resilience4j）、K8s HPA自动扩容规则、健康检查探针、Prometheus监控指标暴露点，汇总成NFR保障全景图
  作用：系统稳定性的"保险单"——万一扛不住了会怎么样、自动恢复了会怎么样。被问"系统挂了怎么办"时甩出这张图
  位置：docs/11-非功能需求保障视图.md


### 视图12-安全架构视图
  覆盖范围：所有端（认证/授权/加密/审计）
  怎么生成：分析项目代码中的认证拦截器（JWT校验Filter）、权限注解（@PreAuthorize）、密码加密方式（BCrypt）、敏感字段脱敏注解（@Sensitive）、审计日志AOP切面，汇总成安全架构图
  作用：安全的"护城河设计图"——密码怎么存的、权限怎么控的、日志怎么记的。安全审计、等保过审时直接提交这张图
  位置：docs/12-安全架构视图.md


### 视图13-客户端分层调用链图
  覆盖范围：桌面端 + 手机端Web（纯客户端内部调用）
  怎么生成：分析客户端项目代码中的UI层（Activity/ViewController/Page）、ViewModel/Store层、Repository层、网络层/本地DB层的依赖关系与调用方向，画出完整的四层调用链
  作用：客户端的"内部构造图"——UI点击后经过几层才到网络请求、缓存命中逻辑在哪一层。改UI逻辑时查这张图确认影响范围
  位置：docs/13-客户端分层调用链图.md


### 视图14-AI模块全链路工作流时序图
  覆盖范围：手机端Web + 服务端（AI对话与工具调用完整闭环）
  怎么生成：分析AI模块代码中的RAG检索调用、LLM接口封装（支持流式/非流式）、Function Calling的工具注册与执行逻辑、会话上下文管理，绘制出用户提问到最终回答的完整时序
  作用：AI模块的"神经回路图"——用户问题是怎么被一步步处理的、工具调用循环了几次、Token花了多少。AI功能异常时查这张图定位断点
  位置：docs/14-AI模块全链路工作流时序图.md


### 视图15-外部系统迁移对照图
  覆盖范围：服务端（对接/替换外部系统时的迁移方案）
  怎么生成：对比新旧外部系统的接口文档、消息格式样例、调用时序差异，汇总成字段映射表、灰度切流方案、回滚预案
  作用：迁移项目的"作战地图"——旧系统切到新系统分几步、每一步怎么验证、失败了怎么回来。做外部系统替换时全程用这张图指挥
  位置：docs/15-外部系统迁移对照图.md


### 视图16-遗留系统改造与缺陷复盘图
  覆盖范围：所有端（历史Bug复盘 + 遗留代码剥离规范）
  怎么生成：汇总过去一年所有P0/P1故障的复盘记录（根因/修复/预防措施），以及从旧版剥离功能时的接口抽取规范、逻辑映射表、回归验证用例集合
  作用：项目的"错题本"加"手术指南"——以前踩过什么坑、怎么修的、下次如何避免。重构遗留模块前先翻这张图
  位置：docs/16-遗留系统改造与缺陷复盘图.md


### 视图17-项目辅助与演进规划总图
  覆盖范围：整个项目（版本变更/文档导航/博客规划/技术演进/图片资源）
  怎么生成：汇总Git Release Notes、文档中心目录树、技术博客排期表、技术栈升级路线图（JDK/K8s/Service Mesh等里程碑）、图片资源管理规范，形成一张综合辅助总图
  作用：项目的"仪表盘"加"望远镜"——当前版本改了啥、下一步往哪走、文档去哪找。项目经理/技术负责人做季度规划时用这张图总览全局
  位置：docs/17-项目辅助与演进规划总图.md


### 视图18-用户界面视图
  覆盖范围：手机端Web + 桌面端（UI全局总览）
  怎么生成：梳理项目的UI设计系统（Figma/Sketch设计稿链接）、组件库目录、设计令牌（颜色/字体/间距）规范、以及多端UI整体关系，形成UI视图的索引入口
  作用：UI设计的"封面图"——整体UI架构长什么样、所有UI相关视图如何索引。产品经理/设计师入职第一周先看这张图
  位置：docs/18-用户界面视图.md


### 视图19-页面导航结构图
  覆盖范围：手机端Web + 桌面端（页面跳转关系）
  怎么生成：分析项目前端/客户端代码中的路由配置（router/index.js、Navigation组件、DeepLink配置），提取所有页面路径、跳转触发条件、路由守卫逻辑，汇总成站点地图
  作用：页面的"地铁线路图"——有哪些页面、互相怎么跳、跳不过去卡在哪一步。新增页面时查这张图决定放哪个层级
  位置：docs/19-页面导航结构图.md


### 视图20-页面布局与组件拆解图
  覆盖范围：手机端Web + 桌面端（核心页面拆解）
  怎么生成：选取3-5个核心页面，分析其前端/客户端代码的组件树结构（Vue/React组件嵌套、Android ViewGroup层级、SwiftUI View组合），标注各组件的数据来源和事件回调，汇总成组件拆解图
  作用：页面的"乐高说明书"——每个页面由哪些零件拼成、每个零件来自哪个组件库。改某个区域UI时查这张图定位对应组件
  位置：docs/20-页面布局与组件拆解图.md


### 视图21-交互状态流转图
  覆盖范围：手机端Web + 桌面端（页面状态机）
  怎么生成：分析项目代码中每个核心页面的加载状态管理（Loading/Content/Empty/Error/Unauthorized）、异常捕获与降级UI的切换逻辑，汇总成五态流转图
  作用：页面状态的"红绿灯"——每种状态下用户看到什么、怎么从出错状态恢复。UI异常（白屏/一直转圈）时查这张图定位状态卡在哪一步
  位置：docs/21-交互状态流转图.md


### 视图22-设计系统/组件库全景图
  覆盖范围：手机端Web + 桌面端（UI组件库与设计令牌）
  怎么生成：分析项目前端/客户端代码中的组件库源码（Button/Input/Modal等所有基础组件）、全局样式变量（CSS Variables / Design Tokens JSON）、暗黑模式变量映射，汇总成组件库全景图
  作用：UI的统一"零件库"——有哪些现成的UI零件、每个零件长什么样、有哪些尺寸/颜色变体。设计和开发保持一致的参照物
  位置：docs/22-设计系统与组件库全景图.md


### 视图23-多端适配对比图
  覆盖范围：手机端Web + 桌面端（跨端UI差异）
  怎么生成：将同一功能页面在手机端、桌面端、Pad端、大屏端的最终实现效果并排截图，标注布局差异（栅格/信息密度）和交互差异（点击vs悬停），汇总成多端对比图
  作用：多端适配的"差异对照表"——同一功能在不同端长得不一样，设计规范是如何分别实现的。做多端需求时查这张图确保体验一致性
  位置：docs/23-多端适配对比图.md


### 视图24-UI渲染管线与帧生命周期图
  覆盖范围：手机端Web + 桌面端（渲染底层机制）
  怎么生成：分析项目前端/客户端代码中的渲染相关配置（浏览器渲染流水线/Android Choreographer/iOS CADisplayLink）、布局与绘制方法的调用时机，绘制VSync信号触发到屏幕显示的完整渲染管线
  作用：UI卡顿的"解剖图"——每一帧16.6ms花在哪了、哪里可能掉帧。排查滑动不流畅/页面卡顿时用的底层地图
  位置：docs/24-UI渲染管线与帧生命周期图.md


### 视图25-UI线程与异步任务调度图
  覆盖范围：手机端Web + 桌面端（线程模型与异步切回主线程的机制）
  怎么生成：分析项目前端/客户端代码中的异步API使用处（Kotlin Coroutine/RxJava/AsyncTask、JavaScript Event Loop/WebWorker、iOS GCD）、线程切换代码（withContext/observeOn/dispatch_async），绘制四类线程的分工与切换路径
  作用：UI卡顿与内存泄漏的"追责图"——耗时操作有没有切到后台线程、子线程能不能更新UI、异步任务在页面销毁时有没有取消。排查ANR/界面冻屏时查这张图
  位置：docs/25-UI线程与异步任务调度图.md


### 视图26-UI事件分发与透传机制图
  覆盖范围：手机端Web + 桌面端（触摸/点击事件路由规则）
  怎么生成：分析项目前端/客户端代码中的事件监听绑定方式、Android TouchEvent拦截/消费方法（onInterceptTouchEvent/onTouchEvent）、Web端事件冒泡与捕获机制，绘制事件从物理触碰到UI响应的完整路由
  作用：UI交互异常的"破案图"——点击为什么没反应、滑动为什么冲突了、上层透明遮罩为什么挡住了下层点击。排查点击穿透/滑动冲突时查这张图
  位置：docs/26-UI事件分发与透传机制图.md


### 视图27-前端路由与全局状态同步图
  覆盖范围：手机端Web + 桌面端（URL/Navigation栈与状态管理仓库的双向绑定）
  怎么生成：分析项目前端/客户端代码中的路由配置与状态管理仓库的连接处（路由守卫中派发Action、页面加载时commit Mutation、页面卸载时清理状态），绘制URL变化→页面切换→Store更新的联动路径
  作用：页面跳转与数据混乱的"推理图"——切换页面时哪些状态刷新了哪些保留了、从详情页回退到列表页列表状态还在不在。排查"页面切走再回来数据不对"时查这张图
  位置：docs/27-前端路由与全局状态同步图.md


### 视图28-跨端UI桥接层视图
  覆盖范围：手机端Web + 桌面端（若使用跨端框架，否则标注不适用）
  怎么生成：分析项目跨端框架代码中的Bridge/Channel/PlatformView注册与调用逻辑（JS→Native消息收发、Flutter MethodChannel、RN Native Modules），绘制JS/Dart层与原生层的通信桥梁及数据序列化格式
  作用：跨端混合渲染的"翻译官图"——JS/Dart怎么调用原生功能、原生怎么回传数据给JS/Dart、大数据传输怎么优化。排查跨端UI与原生交互异常时查这张图
  位置：docs/28-跨端UI桥接层视图.md


通用补充说明：本清单所列视图均为建议绘制的核心视图，可根据项目实际情况灵活调整。若某项视图在当前项目中不适用（如无外部系统迁移需求、无AI模块、无跨端框架等），可标注为“不适用”并跳过绘制，不影响其他视图的完整性。反之，若项目有特殊需求，也可在本清单基础上额外增加自定义视图。