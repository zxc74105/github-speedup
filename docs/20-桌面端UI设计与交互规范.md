# 视图20-桌面端UI设计与交互规范

## 覆盖范围
桌面应用 UI 设计与实现

## 技术选型

| 选项 | 选择 | 原因 |
|------|------|------|
| UI 框架 | **PySide6 (Qt6)** | Python Qt6 官方绑定 |
| UI 实现 | **QWidget 原生控件** | 原生风格，零 Web 依赖 |
| 表格 | **QTableWidget** | 原生表格控件 |
| 图表 | **PyQtGraph / matplotlib** | 实时速度趋势图 |
| 状态管理 | **Python 信号/槽** | 原生 Qt 事件机制 |
| 打包 | **PyInstaller --onefile** | 单 .exe 输出 |

## 实现状态

| 功能 | 状态 |
|------|------|
| 侧栏导航（下载/代理/设置） | ✅ 已实现 |
| 下载页-任务列表表格 | ✅ 已实现 |
| 下载页-新建任务弹窗 | ✅ 已实现 |
| 下载页-实时进度更新（信号/槽） | ✅ 已实现 |
| 下载页-取消/删除任务 | ✅ 已实现 |
| 下载页-筛选按钮组 | ✅ 已实现 |
| 下载页-底部详情面板 | ✅ 已实现 |
| 代理页-代理列表表格 | ✅ 已实现 |
| 代理页-全部测速按钮 | ✅ 已实现 |
| 代理页-代理预检 | ✅ 已实现 |
| 代理页-导入/导出代理 | ✅ 已实现 |
| 代理页-删除选中/搜索 | ✅ 已实现 |
| 代理页-成功代理记录 | ✅ 已实现 |
| 代理页-状态排序（延迟/速度） | ✅ 已实现 |
| 设置页-所有设置项 | ✅ 已实现 |
| 设置页-浏览目录按钮 | ✅ 已实现 |
| 设置页-导出/清除记录 | ✅ 已实现 |
| 设置页-HTTP 状态显示 | ✅ 已实现 |

## 后端 Python API

```python
# DownloadAPI
class DownloadAPI:
    def create_task(self, req: CreateTaskReq) -> TaskInfo: ...
    def get_tasks(self) -> list[TaskInfo]: ...
    def cancel_task(self, id: int) -> None: ...
    def delete_task(self, id: int) -> None: ...
    def get_success_records(self) -> list[ProxyRecord]: ...
    def delete_proxies(self, domains: list[str]) -> None: ...

# ProxyAPI
class ProxyAPI:
    def get_proxies(self) -> list[ProxyItem]: ...
    def test_all_proxies(self) -> None: ...
    def test_proxy(self, domain: str) -> ProxyTestResult: ...
    def toggle_proxy(self, domain: str, enabled: bool) -> None: ...
    def import_proxies(self, file_path: str) -> int: ...
    def export_proxies(self, file_path: str) -> None: ...
    def preflight_check(self) -> PreflightResult: ...
    def get_silent_proxies(self) -> list[str]: ...

# ConfigAPI
class ConfigAPI:
    def get_settings(self) -> Settings: ...
    def save_settings(self, s: Settings) -> None: ...
    def reset_settings(self) -> Settings: ...
    def pick_directory(self) -> str: ...
    def export_records(self) -> str: ...
    def clear_records(self) -> None: ...
```

## 实时事件（Qt 信号/槽）

| 信号名 | 数据结构 | 触发时机 |
|--------|---------|---------|
| `progress_updated` | ProgressData | 下载过程中推送进度/速度 |
