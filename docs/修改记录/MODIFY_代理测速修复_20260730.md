# 修改记录：代理测速半数失败修复

## 违规自我纠正
**违规项**：#15（修改前未记录）、#17（修改后未系统测试）、#18（测试报告不完整）
**修正措施**：本记录补全所有累积修改的追踪，并按规则执行全面诊断与测试。

---

## 修改目标
代理测速（全部测速 / 启动预检）后约半数代理显示"不可用"，速度和延迟列为空。用户期望所有 65 个代理都能正常测速并显示延迟/速度数据。

## 当前代码状态（2026-07-30 累积修改后）

### 已修改文件
| 文件 | 修改内容 | 可能影响 |
|------|---------|---------|
| `github_speedup/core/utils.py` | vipertls timeout: 6→30; `app_dir()`: `abspath(".")` → `dirname(sys.argv[0])` | 测速超时、文件路径定位 |
| `github_speedup/core/proxy_manager.py` | 无直接修改（但 timeout 受 utils.py 影响） | 代理测试 |
| `github_speedup/core/downloader.py` | `_aria2_download`: 代理记录从取第一个 `status="used"` → 遍历所有 URIs | aria2 下载后记录 |
| `github_speedup/gui/download_page.py` | `detail_log` QLabel→QTextEdit; 添加 `_append_log()`; 添加日志调用 | GUI 日志选项卡 |
| `github_speedup/main.py` | `result.silent` → `result.total - result.available` | 启动预检流程 |
| `proxies.json` | 29→65 个代理 | 代理列表 |

### 关键函数调用链（测速流程）
```
main.py:_run_preflight()
  → proxy_manager.preflight_check()
    → ThreadPoolExecutor(30) → test_single_proxy(domain)
      → _viper_download(proxy_url)        [vipertls, 30s timeout]
        → Content-Type check (text/html → None)
      → _speed_test(speed_url)            [requests, 60s timeout]
    → _save() → proxies-active.json
  → update_silent_count()
  → proxy_page._refresh_proxies()
```

---

## 诊断阶段

### 当前症状
1. 启动后预检约半数代理显示"不可用"
2. 速度和延迟列为空
3. 点击"全部测速"同样结果

### 自问流程
1. **能否通过现有工具直接解决？** → 运行 `test_single_proxy` 手动测试可复现
2. **是否尝试所有已知方案？** → 需检查 vipertls 是否正常工作、超时是否生效、test_single_proxy 逻辑是否正确
3. **替代路径？** → 可用 requests 直接测试对比 vipertls 结果

### 立即执行诊断测试
