# 视图20-UI设计与交互规范

## 覆盖范围
终端用户界面 (TUI) 完整设计

## 技术选型

| 选项 | 选择 | 原因 |
|------|------|------|
| UI 框架 | **textual** (Textualize/textual) | Python 原生 TUI 框架；跨平台；CSS 样式 |
| 组件库 | **textual 内置组件** (DataTable/Input/Header/Footer等) | 官方配套组件，零额外依赖 |
| 布局 | **textual CSS 布局** | 内置；支持网格、弹性、dock 布局 |
| 渲染 | 终端 ANSI (无图形依赖) | 无额外依赖，即开即用 |

## 整体布局

```
┌──────────────────────────────────────────────────────────────────────┐
│  📥 Multi-Proxy Downloader  v1.2.0          [IDLE]    2026-07-25   │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                        │
│  ┌─ URL ─────────────────────────────────────────────────────────┐   │
│  │  https://github.com/user/repo/releases/download/v1.0/file.zip  │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  ┌─ 代理列表 ───────────────────────┐  ┌─ Worker 实时状态 ──────────┐ │
│  │  STATUS  │ DOMAIN              │  │  ID │ PROXY         │%   │ │
│  │  ────────┼─────────────────────│  │  ───┼───────────────┼────│ │
│  │  ✅ 45ms │ gh.dpik.top         │  │  #1  │ gh.dpik.top   │ 45 │ │
│  │  ✅ 80ms │ ghproxy.net         │  │  #2  │ fastgit.cc    │ 32 │ │
│  │  ✅ 120ms│ gh-proxy.com        │  │  #3  │ ghproxy.net   │ 60 │ │
│  │  ❌ N/A  │ down.mxw.xx.kg      │  │  #4  │ gh-proxy.com  │ 78 │ │
│  │  ⏳ ..   │ slink.ltd           │  │  #5  │ waiting...    │  0 │ │
│  │  ...     │                     │  │  #6  │ waiting...    │  0 │ │
│  └──────────────────────────────────┘  └────────────────────────────┘ │
│                                                                        │
│  ┌─ 全局进度 ─────────────────────────────────────────────────────┐  │
│  │  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░  62.3%        │  │
│  │  324.5 MB / 521.0 MB  │  ↓ 45.2 Mbps  │  ETA 35s              │  │
│  │  分片: 18/30  │  代理: 8/82 有效  │  错误: 3 ▒░░░░░░░░░░░░░  │  │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  ┌─ 日志 ─────────────────────────────────────────────────────────┐  │
│  │  [15:04:23] INFO  获取文件信息 → name: file.zip, size: 521MB     │  │
│  │  [15:04:23] INFO  代理预检完成 → 8/82 可用                       │  │
│  │  [15:04:24] WARN  Worker #4 超时 → 切换代理                     │  │
│  │  [15:04:25] INFO  Worker #2 完成分片 #7                         │  │
│  │  └────────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  ┌─ 底部操作栏 ─────────────────────────────────────────────────────┐ │
│  │  [Tab:切换面板]  [Enter:开始下载]  [p:暂停]  [q:退出]  [?:帮助]  │ │
│  └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

## 面板详解

### 1. 标题栏 (Header)

```
📥 Multi-Proxy Downloader  v1.2.0          [IDLE]    2026-07-25
```

| 元素 | 内容 | 说明 |
|------|------|------|
| 图标 | 📥 | 固定图标 |
| 标题 | `Multi-Proxy Downloader` | 应用名 |
| 版本 | `v1.2.0` | 当前版本 |
| 状态 | `[IDLE]` / `[PREP]` / `[DL]` / `[PAUSE]` / `[DONE]` / `[ERR]` | 全局状态，颜色编码 |
| 日期 | `2026-07-25` | 当前日期 |

**状态颜色映射：**

| 状态 | 标签 | 颜色 | 含义 |
|------|------|------|------|
| IDLE | `[IDLE]` | 蓝色 | 等待用户输入 |
| PREP | `[PREP]` | 黄色 | 正在解析/预检 |
| DL | `[DOWNLOADING]` | 绿色 | 下载中 |
| PAUSE | `[PAUSED]` | 黄色 | 暂停 |
| DONE | `[COMPLETED]` | 绿色(加粗) | 完成 |
| ERR | `[ERROR]` | 红色 | 出错 |

---

### 2. URL 输入框

```
┌─ URL ─────────────────────────────────────────────────────────┐
│  https://github.com/user/repo/releases/download/v1.0/file.zip  │
└────────────────────────────────────────────────────────────────┘
```

| 交互 | 行为 |
|------|------|
| 聚焦时 | 光标闪烁，可输入/粘贴 URL |
| Enter | 开始解析（触发代理预检） |
| 空状态 | 浅色占位文字 `输入或粘贴下载链接...` |
| 粘贴 | 支持 Ctrl+V / 右键粘贴 |
| 历史 | ↑↓ 键切换历史 URL（最近 10 条） |

后端数据：`fileURL: str`

---

### 3. 代理列表面板

```
┌─ 代理列表 ─────────────────────────────────────────────────────┐
│  # │ STATUS  │ DOMAIN                  │ SPEED    │ 类型     │
│  ──┼─────────┼─────────────────────────┼──────────┼─────────│
│  1 │ ✅ 优   │ gh.dpik.top             │ 45.2 Mbps│ contribute│
│  2 │ ✅ 良   │ ghproxy.net             │ 12.8 Mbps│ contribute│
│  3 │ ✅ 中   │ gh-proxy.com            │ 3.1 Mbps │ search  │
│  4 │ ❌ 超时  │ down.mxw.xx.kg         │ N/A      │ contribute│
│  5 │ ⏳ 测速中 │ slink.ltd              │ ...      │ search  │
│  6 │ ⏸ 跳过  │ 777.z321.cc.cd         │ 已禁用   │ contribute│
│  ...                                                         │
│  82│         │                        │          │         │
│                                                              │
│  已启用: 45 / 82  │  选中: 0  │  排序: 速度 ▼              │
└──────────────────────────────────────────────────────────────┘
```

**状态图标：**

| 图标 | 含义 |
|------|------|
| `⏳` | 测速中 |
| `✅` | 可用 + 显示测速结果 |
| `⚠️` | 可用但较慢（<1 Mbps） |
| `❌` | 不可用 |
| `⏸` | 用户手动跳过 |
| ` ` | 未测速 |

**交互：**

| 操作 | 行为 |
|------|------|
| Tab/↑↓ | 导航行 |
| Space | 切换启用/禁用 |
| `/` | 搜索过滤 |
| `s` | 按速度排序 |
| `r` | 重新测速选中的代理 |
| `R` | 重新测速全部 |
| `a` | 全选 |
| `A` | 取消全选 |

**后端对接：**

```python
@dataclass
class ProxyUIItem:
    domain: str
    group: str          # contribute | search | user
    status: ProxyStatus # Pending | Testing | OK | Slow | Fail | Disabled
    latency_ms: int
    speed_mbps: float
    last_test: datetime
    assigned_worker: int = -1  # -1 = 未分配
```

---

### 4. Worker 实时状态面板

```
┌─ Worker 实时状态 ──────────────────────────────────────────────┐
│  ID │ 代理              │ 分片  │ 大小      │ 速度     │ 进度  │
│  ───┼───────────────────┼───────┼───────────┼──────────┼──────│
│  #1 │ gh.dpik.top       │  5/30 │ 10.0 MB   │ 12.3 Mbps│ ▓▓▓▓░│
│  #2 │ ghproxy.net       │  7/30 │ 10.0 MB   │ 8.1 Mbps │ ▓▓░░░│
│  #3 │ fastgit.cc        │  2/30 │ 6.5 MB    │ 15.7 Mbps│ ▓▓▓░░│
│  #4 │ gh-proxy.com      │ 12/30 │ 10.0 MB   │ 3.2 Mbps │ ▓░░░░│
│  #5 │ [等待中]          │  -    │ -         │ -        │      │
│  #6 │ [等待中]          │  -    │ -         │ -        │      │
│  ...                                                          │
│                                                              │
│  活跃: 4 / 6  │  总下载: 324.5 MB  │  总速度: 39.3 Mbps     │
└──────────────────────────────────────────────────────────────┘
```

| 列 | 说明 |
|----|------|
| ID | Worker 编号 #1-#N |
| 代理 | 当前分配的代理域名（失败后切换会更新） |
| 分片 | 当前分片编号 / 总分片数 |
| 大小 | 该分片已下载的字节 |
| 速度 | 该 Worker 的瞬时速度 |
| 进度 | 微型进度条 + 百分比 |

**后端对接：**

```python
@dataclass
class WorkerStatus:
    id: int
    proxy: str
    part_number: int
    part_size: int
    downloaded: int
    speed: float
    status: WorkerState  # Waiting | Downloading | Retrying | Failed | Done
```

---

### 5. 全局进度面板

```
┌─ 全局进度 ─────────────────────────────────────────────────────┐
│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░  62.3%       │
│  324.5 MB / 521.0 MB  │  ↓ 45.2 Mbps  │  ETA 35s             │
│  分片: 18/30  │  代理有效: 8/82  │  切换次数: 3              │
└────────────────────────────────────────────────────────────────┘
```

| 元素 | 说明 |
|------|------|
| 进度条 | 彩色 ANSI 进度条，62.3% 填充 |
| 数据行1 | 已下载 / 总大小 │ 总速度 │ 预计剩余时间 |
| 数据行2 | 已完成分片 / 总分片 │ 有效代理 / 总量 │ 代理切换总次数 |

**速度图表（按 Tab 可切换至此面板展开）：**

```
  速度趋势 (最近 60s)
  50 ┤        ╱╲
  40 ┤      ╱  ╲  ╱╲
  30 ┤ ╱╲  ╱    ╲╱  ╲
  20 ┤╱  ╲╱          ╲
  10 ┤
      └────────────────────
      0        30s       60s
```

---

### 6. 日志面板

```
┌─ 日志 ─────────────────────────────────────────────────────────┐
│  [15:04:23] INFO   获取文件信息 → name: file.zip, size: 521MB  │
│  [15:04:23] INFO   代理预检完成 → 8/82 可用                    │
│  [15:04:24] WARN   Worker #4 超时 → 从 ghproxy.net 切换到 ...  │
│  [15:04:25] INFO   Worker #2 分片 #7 完成 (10.0 MB)           │
│  [15:04:26] INFO   Worker #1 分片 #5 完成 (10.0 MB)           │
│  [15:04:27] DEBUG  HTTP Range: bytes=31457280-41943039         │
│  [15:04:28] ERROR  Worker #6 下载失败 → 3次重试后放弃          │
│  ...                                                           │
│  [底部:最新消息]                                               │
└────────────────────────────────────────────────────────────────┘
```

| 级别 | 颜色 | 显示规则 |
|------|------|---------|
| DEBUG | 灰色 | 仅 -debug 模式下显示 |
| INFO | 白色/默认 | 始终显示 |
| WARN | 黄色 | 始终显示 |
| ERROR | 红色 | 始终显示 + 状态栏闪烁提示 |

日志自动滚到底部，可 ↑↓ 翻页查看历史。

---

### 7. 底部操作栏

```
┌─ 操作 ──────────────────────────────────────────────────────────┐
│  [Tab:切换面板]  [Enter:开始]  [p:暂停/继续]  [q:退出]  [?:帮助]│
└─────────────────────────────────────────────────────────────────┘
```

**全局快捷键：**

| 键 | 功能 | 可用状态 |
|----|------|---------|
| `Tab` | 切换焦点面板（循环） | 全部 |
| `Enter` | 开始下载 / 确认 | IDLE |
| `p` | 暂停 / 继续 | DL / PAUSE |
| `q` | 退出程序 | 全部（下载中需二次确认） |
| `?` | 打开帮助弹窗 | 全部 |
| `Ctrl+C` | 强制退出 | 全部 |
| `Esc` | 取消/关闭弹窗 | 弹窗打开时 |

**面板内快捷键：**

| 键 | 功能 | 面板 |
|----|------|------|
| `↑/↓` | 移动光标 | 代理列表 / Worker / 日志 |
| `Space` | 切换启用/禁用代理 | 代理列表 |
| `/` | 搜索 | 代理列表 |
| `s` | 按速度排序 | 代理列表 |
| `r` | 重新测速选中代理 | 代理列表 |
| `R` | 全部重新测速 | 代理列表 |

---

## 状态流转

```
                    ┌──────────┐
         ┌─────────▶│  IDLE    │◀────────────┐
         │          └────┬─────┘              │
         │               │ 用户输入 URL + 按 Enter    │
         │               ▼                     │
         │          ┌──────────┐              │
         │          │  PREP    │              │
         │          │ (代理预检) │              │
         │          └────┬─────┘              │
         │               │                    │
         │               ▼                    │
         │          ┌──────────┐              │
         │    ┌────▶│   DL     │────┐         │
         │    │     └────┬─────┘    │         │
         │    │          │          │         │
         │    │   按 p   │ 完成     │ 按 p    │
         │    │          ▼          │         │
         │    │     ┌──────────┐    │         │
         │    └─────│  PAUSE   │────┘         │
         │          └──────────┘              │
         │                                    │
         │               ▼                    │
         │          ┌──────────┐              │
         │          │   DONE   │──────────────┘
         │          └──────────┘    (自动回到 IDLE)
         │
         └── ┌──────────┐
             │   ERR    │
             └──────────┘
```

---

## 弹窗/Overlay 设计

### 帮助弹窗 (?)

```
┌────────────── 帮助 ──────────────────────────────────────────────┐
│                                                                   │
│  全局操作                                                         │
│    Tab       切换面板焦点                                         │
│    Enter     开始下载 / 确认                                      │
│    p         暂停 / 继续                                          │
│    q         退出程序                                             │
│    ?         打开本帮助                                           │
│    Ctrl+C    强制退出                                             │
│                                                                   │
│  代理列表                                                         │
│    ↑/↓       导航行                                               │
│    Space     切换启用/禁用代理                                    │
│    /         搜索过滤                                             │
│    s         按速度排序                                           │
│    r         重新测速选中项                                       │
│    R         全部测速                                             │
│                                                                   │
│  [按任意键关闭]                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 退出确认弹窗 (q)

```
┌──── 确认退出 ────────────────────────────────────────────────────┐
│                                                                   │
│  下载进行中 (62.3%)，确认退出？                                  │
│  未完成的分片将在下次启动时续传。                                │
│                                                                   │
│        [Y: 确认退出]          [N: 取消]                          │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 完成弹窗 (自动消失)

```
┌──── 下载完成 ────────────────────────────────────────────────────┐
│                                                                   │
│  ✅ file.zip 已保存到 D:\downloads\file.zip                      │
│     大小: 521.0 MB                                               │
│     耗时: 1m23s                                                  │
│     平均速度: 42.5 Mbps                                          │
│                                                                   │
│                    [按任意键继续]                                 │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 颜色主题

```python
# 默认浅色主题
THEME = {
    "primary":   "#155DFC",  # 蓝色
    "success":   "#00C853",  # 绿色
    "warning":   "#FFB300",  # 黄色
    "error":     "#FF1744",  # 红色
    "info":      "#00B0FF",  # 浅蓝
    "muted":     "#9E9E9E",  # 灰色
    "accent":    "#AA00FF",  # 紫色
    "bg":        "#FAFAFA",  # 背景
    "border":    "#E0E0E0",  # 边框
}
```

支持暗黑/浅色自动切换（跟随终端主题）。

---

## 数据结构对接

```python
from enum import Enum, auto
from dataclasses import dataclass, field
from threading import Event
from typing import Optional

class AppState(Enum):
    IDLE = auto()
    PREPARING = auto()
    DOWNLOADING = auto()
    PAUSED = auto()
    COMPLETED = auto()
    ERROR = auto()

@dataclass
class UIModel:
    # 面板焦点
    active_panel: int = 0  # 0:URL  1:代理列表  2:Worker  3:日志

    # 输入
    url_input: str = ""
    url_history: list[str] = field(default_factory=list)
    proxy_file_path: str = ""

    # 代理列表
    proxy_items: list[ProxyUIItem] = field(default_factory=list)
    proxy_filter: str = ""

    # Worker 状态
    workers: list[WorkerStatus] = field(default_factory=list)

    # 全局进度
    total_bytes: int = 0
    downloaded_bytes: int = 0
    current_speed: float = 0.0
    eta: float = 0.0
    completed_parts: int = 0
    total_parts: int = 0
    proxy_errors: int = 0

    # 日志
    log_buffer: list[LogEntry] = field(default_factory=list)
    log_offset: int = 0

    # 控制
    state: AppState = AppState.IDLE
    cancel_event: Event = field(default_factory=Event)
```

## 实现路线图

```
第1步: 搭建 textual 骨架
  ├── 新增 github_speedup/tui/app.py (主 App/ compose / on_* 方法)
  ├── 新增 github_speedup/tui/widgets/ (各面板)
  └── main.py: 添加 --tui 参数切换 UI 模式

第2步: 集成现有逻辑
  ├── 将下载循环改为可取消 (threading.Event)
  ├── 通过 queue 将下载进度推送到 TUI
  └── ProxyPool + Worker 状态实时上报

第3步: 代理预检功能
  ├── 启动时/手动触发 → 并发测试所有代理
  ├── HTTP HEAD + 下载 1MB 测试片段
  └── 结果写入 ProxyUIItem.speed_mbps

第4步: 交互完善
  ├── 快捷键系统
  ├── 弹窗系统 (帮助/确认/完成)
  ├── 搜索/过滤/排序
  └── 历史记录持久化

第5步: 高级功能
  ├── 速度趋势图表 (ASCII 绘图)
  ├── 多任务队列 (批量下载)
  └── 配置持久化 (JSON)
```

## 与原 CLI 的兼容关系

```bash
# 原有 CLI 模式 (不变)
python main.py -url "https://..." -proxy proxies.txt -max 30

# 新增 TUI 模式
python main.py --tui
# 在界面中输入 URL、管理代理、查看实时状态

# 或指定 URL 直接进入 TUI 下载
python main.py --tui -url "https://..."
```
